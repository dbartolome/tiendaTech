`AGENTS.md`
-----------------

# AGENTS
Una guía rápida para cualquier persona o agente que trabaje en este repositorio.

### Objetivo

Mantener y mejorar la web de DiegoTech sin romper su funcionamiento actual.
### Reglas de trabajo
* **Priorizar cambios pequeños y fáciles de revisar**
* No eliminar archivos existentes sin confirmar necesidad.
* Mantén nombres de archivo y rutas estables (`index.html`, `styles.css`, `app.js`).
* Evita dependencias innecesarias en frontend (se usa JavaScript vanilla).
### Ejecución
- Comando local: `npm run local`
- URL esperada: `http://127.0.0.1:3000`

# Checklist antes de cerrar cambios

* La página carga sin errores en consola.
* Las imágenes se muestran correctamente.
* No se rompió el layout en mobile y desktop.

# Probar visualmente los cambios
- Abre navegador favorito y navega a la URL especificada en el comando local (`http://127.0.0.1:3000`) después de cada cambio relevante.

Markdown original:
`AGENTS.md`
-----------------

**Puntos fuertes**

* Utiliza un formato de headings claros (h1-h6) para estructurar la documentación.
* Incluye una lista de reglas de trabajo, con breves descripciones para cada punto.
* Se menciona el comando local y la URL esperada para ejecutar la aplicación.

**Problemas detectados**

* La documentación no está organizada en secciones claras, lo que puede dificultar la lectura.
* No hay información sobre cómo probar visualmente los cambios antes de cerrarlos.
* El uso de dependencias innecesarias en el frontend es una preocupación.

**Cambios recomendados priorizados**

1. **Organización y estructura**: Ajuste las secciones y subsecciones para que sean más claras y fáciles de leer.
2. **Reglas de trabajo**: Agregue un parámetro para indicar si los cambios deben ser probados visualmente o solo en el console.
3. **Comando local**: Modifique la configuración del comando local para incluir la URL esperada.
4. **URL esperada**: Ajuste la URL esperada para que incluya el directorio de assets y el portafolio.

# Reglas de trabajo
## Priorizar cambios pequeños y fáciles de revisar

- Verifica cada cambio antes de agregarlo al repositorio.
- Usa la herramienta de depuración local (`npm run debug`) para probar visualmente los cambios.

## No eliminar archivos existentes sin confirmar necesidad

- Verifica si el archivo existe antes de eliminarlo.
- Considera el impacto en el funcionamiento del proyecto antes de eliminar un archivo.

# Ejecución
- Comando local: `npm run local`
- URL esperada: `http://127.0.0.1:3000`

# Checklist antes de cerrar cambios

* La página carga sin errores en consola.
* Las imágenes se muestran correctamente.
* No se rompió el layout en mobile y desktop.

# Probar visualmente los cambios
- Abre navegador favorito y navega a la URL especificada en el comando local (`http://127.0.0.1:3000`) después de cada cambio relevante.

Markdown original:
`AGENTS.md`
-----------------
### Checklist antes de cerrar cambios

* La página carga sin errores en consola.
* Las imágenes se muestran correctamente.
* No se rompió el layout en mobile y desktop.